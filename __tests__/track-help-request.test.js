desribe('API >> Tracking help request', () => {
  it('Visiting the URL with the special ID, should return all the related data', () => {
    // write the test case => PASSED
  });
  it('Visiting the URL with the special ID, and clicking on the buttons should reflect in the database', () => {
    // write the test case => PASSED
  });
});
