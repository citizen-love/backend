desribe('API >> Create Help Offer', () => {
  it('When e-mail preference is set, the help asker should be notified by email', () => {
    // write the test case PASSED
  });
  it('When sms preference is set, the help asker should be notified by sms', () => {
    // write the test case PASSED
  });
  it('When an offer is sent, the help object counter should be incremented', () => {
    // write the test case PASSED
  });
});
