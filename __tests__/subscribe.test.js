desribe('API >> Subscribe to the watchlist', () => {
  it('When subscribing to the watchlist, I should receive a confirmation email', () => {
    // write the test case => PASSED
  });
  it('If someone in my vicinity initiates a help request, I should receive an email having the emailpreference set', () => {
    // write the test case => PASSED
  });
  it('If someone in my vicinity initiates a help request, I should receive an sms having the smspreference set', () => {
    // write the test case => PASSED
  });
});
